<meta charset="UTF-8">
  <title>Sign Up</title>
  <style>
    body {
      font-family: 'Helvetica Neue', sans-serif;
      background-color: #fffaf5;
      color: #333;
      text-align: center;
      padding: 4rem;
    }
    h1 {
      font-size: 3rem;
      color: #d35400;
    }
    .btn {
      display: inline-block;
      padding: 12px 24px;
      margin: 1rem;
      border: none;
      background-color: #d35400;
      color: white;
      text-decoration: none;
      border-radius: 8px;
      font-size: 1rem;
    }
    .btn:hover {
      background-color: #b84300;
    }
  </style>

<h1>Edit Your Profile</h1>

<form action="/users/edit?_method=PUT" method="POST">
  <label>Username:</label>
  <input type="text" name="username" value="<%= user.username %>"><br>

  <label>Email:</label>
  <input type="email" name="email" value="<%= user.email %>" required><br>

  <label>Address:</label>
  <input type="text" name="address" value="<%= user.address %>"><br>

  <label>Phone:</label>
  <input type="text" name="phone" value="<%= user.phone %>"><br>

  <button type="submit">Save Changes</button>

<!-- <form action="/auth/delete" method="POST" onsubmit="return confirm('Are you sure you want to delete your account?');">
  <button type="submit">Delete Account</button>
</form> -->
<button id="delete-button">Delete Account</button>

<!-- Confirmation Modal -->
<div id="delete-confirmation-modal" style="display: none;">
    <div class="modal-content">
        <h2>Are you sure you want to delete your account?</h2>
        <button id="confirm-delete">Yes, Delete</button>
        <button id="cancel-delete">Cancel</button>
    </div>
</div>

<a href="/users/profile">← Back to Profile</a>
<a href="/menu" class="btn">View Menu</a>